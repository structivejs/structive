# StatePropertyRef Module Overview

State property refs represent resolved state paths that bindings observe or
mutate.

- `StatepropertyRef.ts`: Implementation storing the resolved path info, list
  indexes, and helper accessors.
- `types.ts`: Interfaces describing refs and the metadata shared across modules.

# MainWrapper Module Overview

The main wrapper bootstraps Structive templates and hosts global layout glue.

- `MainWrapper.ts`: Initializes the wrapper element, mounts templates, and wires
  component lifecycle callbacks.
- `main.st.html`: Base HTML structure used by the wrapper during rendering.

# PathTree Module Overview

The path tree stores hierarchical representations of state property paths so
bindings can quickly resolve related refs.

- `PathNode.ts`: Node implementation with child traversal and metadata helpers.
- `types.ts`: Type definitions for nodes, indexes, and traversal context.

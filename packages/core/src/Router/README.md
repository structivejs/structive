# Router Module Overview

Routing helpers allow Structive applications to synchronise component state with
URL changes.

- `Router.ts`: Core router implementation handling registration, navigation
  events, and state updates.
- `types.ts`: Interfaces exposing the router contract to bindings and
  application code.

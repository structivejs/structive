# GlobalId Module Overview

Utility helpers for generating reproducible identifiers live in this folder.

- `generateId.ts`: Produces unique yet deterministic IDs used to tag bindings
  and runtime artifacts.

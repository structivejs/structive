# ListIndex Module Overview

This module tracks positional information for list bindings so repeaters can
synchronise DOM nodes with underlying collections.

- `ListIndex.ts`: Core implementation that stores index metadata and provides
  navigation helpers.
- `types.ts`: Interfaces representing index structures shared across the
  binding pipeline.
